import IconButton from "./IconButton"
import { IconType } from "../../assets/Icons"

export default function Notification() {
  return (
    <IconButton icon={IconType.NotificationIcon} tip="Notification"/>
  )
}